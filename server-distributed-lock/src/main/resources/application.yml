dubbo:
  config-center:
    address: zookeeper://r730.host:2181
  application:
    name: server-musicdistributed-lock
spring:
  application:
    name: distributed-lock
  aop:
    auto: true
    proxy-target-class: true