spring:
  application:
    name: server-file
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://jlht.icu:3306/orange_music
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  aop:
    auto: true
    proxy-target-class: true
mybatis:
  type-aliases-package: cn.fan
  mapper-locations: classpath:mapper/*Mapper.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
dubbo:
  application:
    name: server-file
  config-center:
    address: zookeeper://786793.top:2181
fdfs:
  tracker-list: jlht.icu:22122
  pool:
    max-total: 200
mybatis-plus:
  global-config:
    db-config:
      column-format: "`%s`"
      id-type: auto
